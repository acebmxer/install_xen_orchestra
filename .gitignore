CLAUDE.md
.claude/
xo-config.cfg

# SSH keypairs saved by older versions of --deploy (named after the guest
# hostname). Current versions never write these -- the deployment key is
# destroyed when the deploy finishes -- but the pattern stays so a leftover
# file from an earlier run cannot be committed by accident.
xo-deploy-*.key
xo-deploy-*.key.pub

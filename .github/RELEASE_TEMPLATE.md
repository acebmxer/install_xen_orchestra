<!--
Skeleton for this project's GitHub release notes, matching the style
already used in past releases (see e.g. v0.9.0). Written for the person
installing/upgrading, not for a PR reviewer -- see .github/pull_request_template.md
for that. Delete sections that don't apply; keep the heading style.
-->

## vX.Y.Z

### <Headline for the main change, if there is one, e.g. "New: optional plugin installer">

<!-- One paragraph, plain language, what a user can now do or what changed for them. -->

### Added

-

### Changed

-

### Fixed

-

<!--
Closing line: what an existing user needs to do to upgrade. Most releases
here need nothing beyond `--update` -- say so explicitly rather than
leaving it unstated. If something does need doing (a config change, a
one-time migration step), spell it out.
-->
No action needed to upgrade — run `--update` as usual.
